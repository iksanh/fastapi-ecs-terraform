version: "3.9"

services:
  app:
    build: .
    env_file:
      - .env.docker
    container_name: fastapi_app
    ports:
      - "8000:8000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
